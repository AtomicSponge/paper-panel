/**
 * paper-panel
 * By:  Matthew Evans
 * See LICENSE.md
 */

/**
 * Scans for worlds not yet imported
 */
export const onScan = async () => {
  console.log('Scanning for worlds...')
  if(false) {
    return { errorMessage: '' }
  }
}
