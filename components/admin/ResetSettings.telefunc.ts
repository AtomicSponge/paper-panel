/**
 * paper-panel
 * By:  Matthew Evans
 * See LICENSE.md
 */

/**
 * Resets paper-panel settings
 */
export const onResetSettings = async () => {
  console.log('Reset settings...')
  if(false) {
    return { errorMessage: '' }
  }
}
