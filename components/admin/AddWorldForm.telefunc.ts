/**
 * paper-panel
 * By:  Matthew Evans
 * See LICENSE.md
 */

/**
 * Adds a world to the database
 */
export const onSaveNewWorld = async () => {
  console.log('Adding new world...')
  if(false) {
    return { errorMessage: '' }
  }
}
