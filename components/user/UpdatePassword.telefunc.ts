/**
 * paper-panel
 * By:  Matthew Evans
 * See LICENSE.md
 */

/**
 * Adds a world to the database
 */
export const onPasswordUpdate = async () => {
  console.log('Updating password...')
  if(false) {
    return { errorMessage: '' }
  }
}
